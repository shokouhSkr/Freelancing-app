import { ProjectsHeader, ProjectsTable } from "@/features";

const ProjectsPage = () => {
  return (
    <div>
      <ProjectsHeader />
      <ProjectsTable />
    </div>
  );
};

export default ProjectsPage;
